version: '3.8'

services:

  app:
    image: itelmenko/devops-for-programmers-project-74
    build:
      context: .
      dockerfile: Dockerfile.production
      args:
        - UID=${LOCAL_UID:-1000}
        - GID=${LOCAL_GID:-1000}
